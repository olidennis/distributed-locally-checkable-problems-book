\documentclass[11pt,oneside]{book}

\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage[colorlinks=true,linkcolor=blue,urlcolor=blue]{hyperref}
\usepackage{cleveref}
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{framed}
\usepackage[most]{tcolorbox}
\usepackage[dvipsnames]{xcolor}

\newcommand{\poly}{\operatorname{poly}}

\newcommand{\A}{\mathsf{A}}
\newcommand{\B}{\mathsf{B}}
\newcommand{\C}{\mathsf{C}}
\newcommand{\E}{\mathsf{E}}
\newcommand{\G}{\mathsf{G}}
\newcommand{\I}{\mathsf{I}}
\newcommand{\M}{\mathsf{M}}
\renewcommand{\O}{\mathsf{O}}
\renewcommand{\P}{\mathsf{P}}
\newcommand{\Q}{\mathsf{Q}}
\newcommand{\R}{\mathsf{R}}
\newcommand{\V}{\mathsf{V}}
\newcommand{\X}{\mathsf{X}}
\newcommand{\Y}{\mathsf{Y}}
\newcommand{\p}{\mathsf{+}}
\newcommand{\s}{\mspace{5mu}}
\DeclareMathOperator{\re}{\mathcal R}
\DeclareMathOperator{\rere}{\overline{\mathcal R}}
\newcommand{\ccs}{\mathfrak{C}}
\newcommand{\ccc}{\mathcal{C}}

\newcommand{\retor}{https://roundeliminator.github.io/re-experimental/}

\newtcbox{\roundbox}{
  on line,
  boxrule=0.4pt,
  arc=2pt,
  boxsep=1pt,
  left=2pt,
  right=2pt,
  top=1pt,
  bottom=1pt,
  colback=white,
  colframe=black
}
\newcommand{\mroundbox}[1]{\text{\roundbox{$#1$}}}

\definecolor{darkblue}{RGB}{0,0,139}
\newcommand{\retorcommand}[1]{\textcolor{darkblue}{#1}}


\newtheorem{theorem}{Theorem}

\newcounter{open}
\newtheorem{oq}[open]{Open Problem}
\crefname{open}{Open Problem}{Open Problems}
\Crefname{open}{Open Problem}{Open Problems}

\title{Locally Checkable Problems\\in Distributed Computing}
\author{Dennis Olivetti}
\date{Last Update: \today}

\begin{document}

\frontmatter
\hypersetup{pageanchor=false}
\maketitle
\clearpage
\hypersetup{pageanchor=true}
\tableofcontents

\mainmatter
\chapter{Introduction}
The goal of this book is to provide an overview of recent results about locally checkable problems in the distributed setting. 
Locally checkable problems are problem for which, if we are given a solution, we can efficiently check its validity. 
We will mainly consider the LOCAL model of distributed computing, a synchronous message-passing model, where a graph represents a communication network, and neither the size of the messages nor the computational power of the machines is restricted.
The considered complexity measure is the number of communication rounds required to solve a given problem.
We will address the following topics:
\begin{itemize}
    \item Distributed Complexity Theory. 
    \begin{itemize}
        \item What are the possible distributed time complexities of locally checkable problems?
        \item Does randomness help? How much? Is shared randomness more powerful than private randomness? How about quantum?
        \item Can we automate our work, that is, can we automatically determine the complexity of a given problem?
    \end{itemize}
    \item Lower Bound Techniques.
    \begin{itemize}
        \item We will mainly talk about the round elimination technique, which has been used to prove many lower bounds over the years.
        \item We will discuss other techniques, such as Mark's technique, KMW, and indistinguishability arguments.
    \end{itemize}
    \item Connections with other fields.
    \begin{itemize}
        \item There are deep connections between the distributed setting and a mathematical field called \emph{descriptive combinatorics}.
        \item There are deep connetions bewtween the distributed setting and \emph{finitary factors of iid processes}, a topic coming from the area of analysis of randomized processes.
    \end{itemize}
\end{itemize}


\chapter{Preliminaries}
\section{The LOCAL model}
We model a network as a graph $G = (V,E)$, where the nodes represent machines, and the edges represent communication links. Nodes may be assigned unique IDs.
This model works as follows:
\begin{itemize}
    \item Time proceeds in synchronous steps (machines share a clock).
    \item At the beginning, each node only knows its own input and its degree. The input of a node can be, e.g., its ID, the knowledge of the size $n= |V|$ of the graph, the maximum degree $\Delta$, and/or some additional problem-specific inputs.
    \item Then, computation proceeds in rounds. At each round, each node can exchange messages with its neighbors, and update its own local state (each node has its own private memory, no memory is shared between nodes).
    \item Each node must eventually produce an output.
\end{itemize}
The complexity of an algorithm is the worst-case number of rounds that it requires to terminate. This complexity is often measured as a function of $n$ and $\Delta$.

In the LOCAL model, messages can be arbitrarily large, and computation is unbounded. There are many variants of the LOCAL model that one can consider, and they may have different power:
\begin{itemize}
    \item Is $n$ known by the nodes? Is a polynomial upper bound on $n$ known?
    \item Do nodes have IDs?
    \item Do nodes have access to randomness? How much randomness? A finite or infinite bit-string? Do we require Las-Vegas or Monte-Carlo algorithms?
    \item Messages are arbitrarily large, but are they finite? Can we e.g.\ send real numbers?
    \item Can nodes locally perform uncomputable computation?
\end{itemize}


\section{Locally Checkable Problems}
Locally Checkable Problems (LCP) are problems for which it is easy to check whether a given solution is valid. In more detail, a locally checkable problem is a problem for which there exists a constant-time distributed algorithm called \emph{verifier} satisfying that, given a solution:
\begin{itemize}
    \item If the solution is correct, then the algorithm outputs \emph{yes} on all nodes.
    \item If the solution is incorrect, then the algorithm outputs \emph{no} on at least one node.
\end{itemize}
Many interesting problems are locally checkable, for example:
\begin{itemize}
    \item Coloring problems. For example, consider the $(\Delta+1)$-coloring problem, and assume that the verifier knows $\Delta$. The verifier requires $1$ round of communication, in which it checks that the color of the current node is in $\{1,\ldots,\Delta+1\}$, and that it is different from the color of the neighbors.
    \item Maximal independent set. The verifier needs to check that if the current node is in, then all its neighbors must be out, and that if the current node is out, then at least one neighbor is in. Observe that the \emph{Maximum} independent set problem is not locally checkable.
    \item Single source shortest paths (SSSP), or at least some variants of it. Consider for example the variant in which each node outputs its own distance and a pointer to the parent. Then the verifier needs to check that the distance of the parent, plus the weight of the edge connecting the node to the parent, is equal to the distance of the node, and that no other neighbor would give a strictly smaller distance.
    \item Sinkless orientation, that is, orienting the edges so that each node does not have all edges oriented incoming.
    \item List-coloring, where each node is given a list of colors as input, and each node needs to output a color from its own list, such that the resulting coloring is proper.
\end{itemize}

\section{LCLs}
In \cite{naor-stockmeyer-1995-what-can-be-computed-locally}, a restricted class of LCP, called Locally Checkable Labelings (LCLs), was introduced. This class is restricted enough so that we can prove interesting results, but wide enough to still contain many problems of interest. In more detail, LCLs are LCPs with the following restrictions:
\begin{itemize}
    \item The number of input and output labels is constant (i.e., it does not depend on $n$).
    \item It is assumed that $\Delta \in O(1)$.
\end{itemize}
An example of a problem that is an LCP but not an LCL is SSSP, because nodes need to output distances, and distances could depend on $n$.

The nice thing about this class of problems is that each problem has a finite description:
\begin{itemize}
    \item Let $r$ be the number of rounds taken by the verifier. Clearly, the output of the verifier can only depend on things at distance at most $r$ from the node. 
    \item Since $\Delta$ and $r$ are in $O(1)$, and the number of labels is constant, there is a finite number of graphs that the verifier could see.
    \item Thus, we can list the ones that the verifier would accept. Hence, we can describe a problem by listing the accepted neighborhoods.
\end{itemize}

\section{Distributed Complexity Theory}
Being LCLs so restrictive, researchers managed to prove many interesting properties about them. Moreover, many techniques that have been initially developed to understand specific LCLs, have then been generalized to understand problems that fall outside of this class (e.g., round elimination).
Common questions are the following:
\begin{itemize}
    \item What are possible LCL complexities?
    \item Can we automatically decide the complexity of a given LCL?
    \item Does shared/private randomness help?
    \item Does quantum help?
\end{itemize}

\subsection{Paths and Cycles}
In the case of paths and cycles, LCLs have 3 possible complexities \cite{balliu-brandt-etal-2019-the-distributed-complexity-of,brandt-hirvonen-etal-2017-lcl-problems-on-grids,chang-studeny-suomela-2021-distributed-graph-problems}:
\begin{itemize}
    \item $O(1)$. Problems with this complexity often admit very easy algorithms. In the case of directed cycles with no inputs, we even know that all problems with this complexity can be solved in $0$ rounds.
    \item $\Theta(\log^\star n)$. Problems with this complexity require breaking symmetry, but do not require global coordination. Examples are $3$-coloring and MIS.
    \item $\Theta(n)$ and unsolvable problems. Problems with this complexity require global coordination. An example is $2$-coloring, where, by fixing the output of a single node, we are forcing the output on all other nodes.
\end{itemize}
Surprisingly, LCLs on paths and cycles cannot have any other possible complexity, and randomization never helps. This fact has the following interesting implication: you can be sloppy. Suppose you prove that your favourite problem can be solved in $O(\log n)$ rounds via a simple randomized algorithm. You automatically get that the problem can also be solved in $O(\log^\star n)$ by a more clever algorithm, deterministically.

Another surprising fact is that everything is decidable, even for LCLs with inputs. In other words, we can feed the description of an LCL to a computer program, and the program is going to output the correct complexity, and a description of an optimal algorithm.

\subsection{Trees}
In the case of trees, there are the same possible complexities of paths and cycles, plus some more \cite{chang-pettie-2019-a-time-hierarchy-theorem-for-the,balliu-brandt-etal-2018-almost-global-problems-in-the,chang-2020-the-complexity-landscape-of-distributed}:
\begin{itemize}
    \item $\Theta(\log n)$ deterministic. These problems can either be $\Theta(\log n)$ randomized, or $\Theta(\log \log n)$ randomized.
    \item $\Theta(n^{1/k})$ for any $k \in \mathbb{N}^{+}$. For these problems, randomness does not help.
\end{itemize}
No other complexity is possible. Moreover, it is decidable whether a problem can be solved in $O(\log n)$ or not, and in the latter case, what the exact value of $k$ is. Summarizing, some things are still decidable, and randomness can only help exponentially or not at all, and if it helps, it only helps for problems with deterministic complexity $\Theta(\log n)$. A major open question is whether it is decidable if a problem is $\Omega(\log n)$.

\subsection{General graphs}
In general graphs, things are entirely different. For deterministic complexities, the following is known \cite{chang-kopelowitz-pettie-2016-an-exponential-separation,balliu-hirvonen-etal-2018-new-classes-of-distributed,balliu-brandt-etal-2018-almost-global-problems-in-the}:
\begin{itemize}
    \item There are problems with complexity $O(1)$.
    \item No problem with complexity in $\omega(1)-o(\log \log^\star n)$ exists.
    \item The region $\Omega(\log \log^\star n)-O(\log^\star n)$ is dense: informally, for any complexity in this range, we can find a problem with a complexity that is arbitrarily near to it.
    \item No problem with complexity in $\omega(\log^\star n)-o(\log n)$ exists.
    \item The region $\Omega(\log n)$ is dense.
\end{itemize}
In the case of general graphs, undecidability results are known. Very informally, they are proved by showing that it is possible to define problems that require outputting the execution of a Turing machine, and whether the problem is constant or not depends on the running time of the machine. About randomness, a surprising connection is known: if a problem has randomized complexity $o(\log n)$, then it has randomized complexity $O(T_\mathrm{LLL})$, where $T_\mathrm{LLL}$ is the distributed time complexity of the constructive Lov\'asz Local Lemma \cite{chang-pettie-2019-a-time-hierarchy-theorem-for-the}. It is known that $T_\mathrm{LLL}$ is in $\Omega(\log \log n)$ and in $O(\poly \log \log n)$, and a big open question is determining the exact complexity of LLL. Moreover, it is known that randomness can help in different regions. For example, it is known that, for all $k \in \mathbb{N}$, there are problems with deterministic complexity $\Theta(\log^{k+1} n)$ and randomized complexity $\Theta(\log^k n \log \log n)$ \cite{balliu-brandt-etal-2020-how-much-does-randomness-help}.

\section{Easy vs Hard}
A central question about LCLs is to determine whether a problem is \emph{easy} or \emph{hard}, which we define as follows.
\begin{itemize}
    \item Easy problems are problems that can be solved in $O(\log^\star n)$. We call these problems easy because, in order to solve them, we do not need to exploit structural properties of the graph, and only some basic symmetry breaking is required. In some sense, these problems are truly local.
    \item If a problem cannot be solved in $O(\log^\star n)$, then we know it must have deterministic complexity $\Omega(\log n)$. If a problem falls in this latter case, then we say that the problem is hard. Problems of this type cannot be solved with local information: an algorithm running in $\Omega(\log n)$ rounds (for some large-enough constant hidden in the $\Omega$ notation) is guaranteed to see at least a node of degree $\le 2$ or a cycle. Algorithms of this type are not truly local; they need to see \emph{something} in order to be able to solve the problem. Moreover, problems of this type have randomized complexity $\Omega(\log \log n)$. Observe that this is the sweet spot to obtain high-probability guarantees: either the algorithm sees \emph{something} in the structure of the graph that it may be able to exploit, or it sees $\Omega(\log n)$ nodes. In the latter case, consider some randomized process that has constant success probability independently on each node. By seeing $\Omega(\log n)$ nodes, the algorithm will see a successful event with high probability, which it may be able to exploit.
\end{itemize}

\section{Black-White formalism}
An important technique used to prove lower bounds in the LOCAL model is the Round Elimination technique \cite{brandt-2019-an-automatic-speedup-theorem-for}. This technique does not directly work on LCLs, but requires the problem to be described with a specific language, called black-white formalism. It turns out that, at least on trees, this is not a restriction: we can convert any given LCL into a problem in the black-white formalism, and the two problems are equivalent, up to $O(1)$ additive rounds \cite{balliu-censor-hillel-etal-2021-locally-checkable}. On general graphs, problems in the black-white formalism are a strict subclass of LCLs, but most natural problems can anyway be expressed in the black-white formalism.


\chapter{Distributed Complexity Theory}
[TODO: summarize all papers about LCLs, with proof sketches]

\chapter{Connections with Other Fields}
[TODO: descriptive combinatorics, ffiid]

\chapter{Black-White Formalism}
The black-white formalism allows to define (some) LCLs in a concise way.
While there are many variants that we will consider later, the most basic setting is the following:
\begin{itemize}
    \item We assume that we are in a $(\Delta,\delta)$-biregular graph, where white nodes have degree $\Delta$ and black nodes have degree $\delta$. 
    \item There are no input labels.
    \item We need to output one label on each edge.
    \item We have a set (called \emph{white constraint}) of \emph{white allowed configurations}, which are multisets of size $\Delta$ that denote valid labelings of the edges incident to the white nodes.
    \item We have a set (\emph{black constraint}) of \emph{black allowed configurations}, which are multisets of size $\delta$ that denote valid labelings of the edges incident to the black nodes.
\end{itemize}
In other words, a problem $\Pi$ in the black-white formalism is a triple $(\Sigma, W,B)$, where $\Sigma$ is the (finite) set of possible labels, $W$ is a finite multiset, where each multiset has size $\Delta$, and $B$ is a multiset, where each multiset has size $\delta$.
Note that the definition of a problem $\Pi$ is just this, in particular $\Sigma$ is finite and there is no $n$ involved: we cannot define problems where the number of labels depends the size of the graph.

\section{Sinkless Orientation on Bipartite Graphs}
Let's see a simple example (illustrated in \Cref{fig:bipartite-so}). Consider the following setting: we have the promise that our graph is $3$-regular and $2$-colored. We want to define a problem that requires to orient the edges of the graph such that no node is a sink, that is, each node must have at least one edge oriented outwards.
We can define this problem in the black-white formalism as follows.
\begin{itemize}
    \item $\Sigma = \{\A,\B\}$
    \item $W = \{\{\A,\A,\A\},\{\A,\A,\B\},\{\A,\B,\B\}\}$
    \item $B = \{\{\B,\B,\B\},\{\B,\B,\A\},\{\B,\A,\A\}\}$
\end{itemize}
The intuition is the following:
\begin{itemize}
    \item Each edge gets exactly one label, $\A$ or $\B$. If an edge is labeled $\A$, then the edge is oriented from the white node to the black node, while if an edge is labeled $\B$, then the edge is oriented from the black node to the white node.
    \item The white constraint $W$ allows every configuration of $3$ labels that contains at least one $\A$, that is, at least one edge oriented away from the white node.
    \item The black constraint $B$ allows every configuration of $3$ labels that contains at least one $\B$, that is, at least one edge oriented away from the black node.
\end{itemize}
In order to describe problems in the black-white formalism more compactly, we use the following notation.
Instead of writing, e.g., $\{\A,\A,\A\}$, we simply write $\A \s \A \s \A$, or even $\A^3$. Hence, the white constraint becomes $\{\{\A^3\},\{\A^2 \s \B\},\{\A \s \B^2\}\}$. To make the notation even more compact, we write these three allowed configurations as a single one: $\A \s [\A\B]^2$. Think of it as a regular expression: we pick an $\A$, and then we can pick $\A$ or $\B$, twice. We call this type of configurations, \emph{condensed configurations}. Sometimes we even omit the square brackets, and we just write $\A \s \A\B^2$. This is the format that \emph{round eliminator} (the tool that automatically applies the round elimination technique) expects.
In order to describe a whole problem compactly, we omit $\Sigma$, we write a condensed configuration on each line, and we separate the white and the black constraint by an empty line. Hence, sinkless orientation is compactly defined as follows:
\begin{flalign*}
& \A \s \A\B^2 &\\
\\
& \B \s \A\B^2 &
\end{flalign*}



\begin{figure}[ht]
\centering
\begin{subfigure}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figs/bipartite so 1.pdf}
\caption{A solution of bipartite sinkless orientation on bipartite $2$-colored graphs.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figs/bipartite so 3.pdf}
\caption{The same solution, encoded in the black-white formalism.}
\end{subfigure}
\caption{On the left, a solution of bipartite sinkless orientation as one would naturally imagine it. On the right, the same solution encoded in the black-white formalism. Each edge oriented from a white node to a black node gets the label $\A$, while each edge oriented from a black node to a white node gets the label $\B$.}
\label{fig:bipartite-so}
\end{figure}

\section{Node-Edge Formalism}
The above example was in bipartite $2$-colored graphs. However, the same formalism can be used to define problems in standard graphs. The idea is to just take $\delta = 2$ (see \Cref{fig:node-edge}). Observe that, in this case, each black node is in the middle of an edge connecting two white nodes. Let's just ignore these black nodes and pretend they do not exist. We get the following setting:
\begin{itemize}
    \item We assume that we are in a $\Delta$-regular graph. 
    \item There are no input labels.
    \item We need to output one label on each \emph{half-edge} (node-edge pair). In other words, we need to output two labels for each edge, one from each side.
    \item We have a list of \emph{allowed node configurations}, which are multisets of size $\Delta$ that denote valid labelings of the edges incident to the nodes.
    \item We have a list of \emph{allowed edge configurations}, which are multisets of size $2$ that denote valid labelings of the edges.
\end{itemize}
This special case of the black-white formalism is called \emph{node-edge formalism}.

\begin{figure}[ht]
\centering
\begin{subfigure}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figs/node-edge 1.pdf}
\caption{A $(3,2)$-biregular graph.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figs/node-edge 2.pdf}
\caption{A $3$-regular graph.}
\end{subfigure}
\caption{By considering the graph on the left, and ignoring black nodes, we obtain the graph on the right. Observe that a labeling of the edges of the graph on the left corresponds to a labeling of the half-edges of the graph on the right.}
\label{fig:node-edge}
\end{figure}

\section{Sinkless Orientation}
Let's see an example of a problem encoded in the node-edge formalism (see \Cref{fig:so}).
We can define sinkless orientation as follows:
\begin{flalign*}
& \O \s \I\O^2 &\\
\\
& \I \s \O &
\end{flalign*}
In other words:
\begin{itemize}
    \item The labels are $\I$ (oriented incoming) and $\O$ (oriented outgoing).
    \item Each node must have at least one $\O$, that is, one edge oriented outgoing. But also two or three outgoing edges are fine, so all choices over the regular expression $\O \s \I\O^2$, that is, $\O \s \I \s \I$, $\O \s \O \s \I$, $\O \s \I \s \O$, $\O \s \O \s \O$ are allowed. Note that $\O \s \O \s I$ and $\O \s \I \s \O$ are the same multiset, because the order does not matter.
    \item Edges must be properly oriented: if a node labels an edge $\I$, then the other endpoint of the edge must label the edge $\O$, and vice versa. We do not write $\O \s \I$ in the list of allowed configurations because it is the same multiset as $\I \s \O$, and the order does not matter.
\end{itemize}

\begin{figure}[ht]
\centering
\begin{subfigure}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figs/sinkless 1.pdf}
\caption{A solution of sinkless orientation.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figs/sinkless 3.pdf}
\caption{The same solution, encoded in the node-edge formalism.}
\end{subfigure}
\caption{On the left, a solution of sinkless orientation as one would naturally imagine it. On the right, the same solution encoded in the node-edge formalism. Each edge gets the label $\O$ on the side oriented outgoing and $\O$ on the side oriented incoming.}
\label{fig:so}
\end{figure}

\section{Coloring}
Let's see another example, $3$-coloring $3$-regular graphs (see \Cref{fig:coloring}).
We can define it in the node-edge formalism as follows.
\begin{flalign*}
& \R^3 &\\
& \G^3 &\\
& \B^3 &\\
\\
& \R \s \G\B &\\
& \G \s \B &
\end{flalign*}
In other words:
\begin{itemize}
    \item Nodes can be colored red, green, or blue.
    \item Nodes colored, e.g., red, must output $\R$ on each of the three incident half-edges.
    \item On the edges, any configuration not consisting of the same color twice is allowed, that is, $\R \s \G$, $\R \s \B$, and $\G \s \B$ are allowed, but, e.g., $\R \s \R$ is not.
\end{itemize}

\begin{figure}[ht]
\centering
\begin{subfigure}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figs/coloring.pdf}
\caption{A solution of $3$-coloring in $3$-regular graphs.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figs/coloring2.pdf}
\caption{An example of encoding in the node-edge formalism. The central node would, e.g., output $\R \s \R \s \R$.}
\end{subfigure}
\caption{A solution of $3$-coloring in $3$-regular graphs, as one would naturally imagine it, and how it is encoded in the node-edge formalism.}
\label{fig:coloring}
\end{figure}

\section{Maximal Independent Set}
Let's now consider a problem that is slightly harder to encode, Maximal Independent Set (MIS). In this problem, we need to select a subset of nodes that satisfy independence and maximality. While it may feel natural to try to encode it by using two labels ($\I$ and $\O$, which stand for ``in the set'' and ``out, not in the set''), this is not possible. 
In more detail, we could try to require nodes in the set to output $\I$ on each incident half-edge, and nodes not in the set to output $\O$ on each incident half-edge. Note that this would not work:
\begin{itemize}
    \item There are valid solutions in which two nodes not in the set are neighbors, and hence we would need to allow $\O \s \O$ on the edges.
    \item Hence, the node constraint contains the configuration $\O \s \ldots \s \O$, while the edge constraint contains the configuration $\O \s \O$.
    \item This implies that the obtained problem can be solved in $0$ rounds of communication, by outputting $\O$ everywhere.
    \item The obtained problem is not \emph{maximal independent set}, but just \emph{independent set}!
\end{itemize}
In other words, we need one more label to ensure maximality. We can define MIS in the node-edge formalism, on $3$-regular graphs, as follows.
\begin{flalign*}
& \M^3 &\\
& \P \s \O^2 &\\
\\
& \M \s \O\P &\\
& \O \s \O &
\end{flalign*}
That is, MIS is encoded as follows:
\begin{itemize}
    \item Nodes in the set output $\M^3$.
    \item Nodes not in the set need to \emph{prove} that they have at least one neighbor in the set, by outputting $\P$ towards them. On the other two edges, they output $\O$.
    \item On the edges, we can see that $\P$ is only compatible with $\M$ (that is, the configuration $\M \s \P$ is the only one containing $\P$), this ensures maximality. Then, nodes not in the set could have more than one neighbor in the set, and hence we allow $\M \s \O$. Moreover, nodes not in the set could be neighbors, and hence we allow $\O \s \O$.
\end{itemize}
See \Cref{fig:mis} for an example.
\begin{figure}[ht]
\centering
\begin{subfigure}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figs/mis3.pdf}
\caption{A solution of MIS.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figs/mis.pdf}
\caption{The same solution, encoded in the node-edge formalism.}
\end{subfigure}
\caption{On the left, a solution of MIS in a $3$-regular graph, as one would naturally imagine it. On the right, the same solution encoded in the node-edge formalism. Each node in the set outputs $\M^3$, while each node not in the set outputs $\P$ towards a neighbor that is in the set, and $\O$ towards the other two neighbors.}
\label{fig:mis}
\end{figure}

Let's call the usual MIS problem standard-MIS, and the version encoded in the node-edge formalism encoded-MIS.
It is important to notice that standard-MIS and encoded-MIS are not exactly the same problem:
\begin{itemize}
    \item Given a solution to encoded-MIS, we can immediately infer a solution to standard-MIS.
    \item However, given a solution to standard-MIS, it takes $1$ round of communication to obtain a solution to encoded-MIS, because nodes not in the set would not otherwise know where to output $\P$.
\end{itemize}
We need to keep this in mind when proving lower bounds: if we prove that encoded-MIS requires at least $T$ rounds to be solved, we only obtain a lower bound of $T-1$ rounds for standard-MIS. While in the case of MIS it does not matter (asymptotically), for some other problems it could matter, so we have to keep this in mind.

\section{Bipartite Maximal Matching}
Let's see another example in the black-white formalism, by considering the problem of computing a maximal matching on bipartite $2$-colored graphs (BMM).
Similarly as in the case of MIS, one may be tempted to encode BMM by using two labels, but this is not possible, we need at least three. We can encode BMM, on $(3,3)$-biregular graphs, as follows:
\begin{flalign*}
& \M \s \O^2 &\\
& \P^3 &\\
\\
& \M \s \O\P^2 &\\
& \O^3 &
\end{flalign*}
See \Cref{fig:bmm} for an example. The intuition is the following:
\begin{itemize}
    \item Edges that are part of the matching are labeled $\M$.
    \item Then, if a white node is not matched, it needs to prove that all its neighbors are matched, so it points to all of them, by outputting $\P$ on all its incident edges.
    \item There are two types of black nodes, matched and unmatched. If a node is matched then it has exactly one edge labeled $\M$, and then it can \emph{accept} pointers. So all its other edges could be $\P$ or $\O$. If a black node is not matched, then it needs to prove that all its white neighbors are matched. We do not need a new type of pointer for this, we can use $\O$, because all non-matched edges incident to white matched nodes are labeled $\O$.
\end{itemize}
Note that this is not the only possible way to encode BMM. One could use a more ``symmetric'' and intuitive version, as follows:
\begin{flalign*}
& \M \s \P\O^2 &\\
& \P^3 &\\
\\
& \M \s \P\O^2 &\\
& \O^3 &
\end{flalign*}
The intuition is the following:
\begin{itemize}
    \item Matched edges get label $\M$, then the other edges are either \emph{white pointers} $\P$ or \emph{black pointers} $\O$.
    \item If a node is matched, then it outputs exactly one $\M$, and then on the other edges everything else is allowed.
    \item If a white node is not matched, it outputs white pointers everywhere, that is, $\P^3$.
    \item If a black node is not matched, it outputs black pointers everywhere, that is, $\O^3$.
    \item Observe that if a white node is not matched, then all its edges are labeled $\P$, and all black configurations containing $\P$ also contain $\M$. Hence, all black neighbors of the white node are matched. The case of unmatched black nodes is symmetric.
\end{itemize}
It turns out that the two variants that we have seen are equivalent, and it is now time to discuss an important detail: who outputs the labels?
We will use the black-white formalism to prove upper and lower bounds via round elimination. In such a setting, it will be convenient to consider the following computational model:
\begin{itemize}
    \item White nodes output labels on their incident edges.
    \item Black nodes participate in the computation, but they do not output anything.
    \item Hence, it is the job of the white nodes to output labels that satisfy, at the same time, white and black constraints on all nodes.
\end{itemize}

Now, a solution for the first variant \emph{is also} a solution for the second variant. A solution for the second variant \emph{can be converted}, without communication, into a solution for the first variant, if we consider the computational model just discussed: matched white nodes just need to change all their $\P$ into $\O$. It is easy to see that the obtained solution is valid.

As a side note, if we enter the second variant of BMM into round eliminator, it automatically transforms the given problem into the first variant, because it sees the $\P$ in the condensed configuration $\M \s \P\O^2$ of the white constraint as useless.
\begin{figure}[ht]
\centering
\begin{subfigure}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figs/matching1.pdf}
\caption{A solution of BMM.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figs/matching2.pdf}
\caption{The same solution, encoded in the black-white formalism.}
\end{subfigure}
\caption{On the left, a solution of BMM in a $3$-regular graph, as one would naturally imagine it. On the right, the same solution encoded in the node-edge formalism. Edges in the matching get the label $\M$, then we need to use $\P$ and $\O$ to ensure maximality.}
\label{fig:bmm}
\end{figure}


\section{Ruling Sets}
Ruling sets are a family of problems parameterized by two positive integers $\alpha$ and $\beta$. An $(\alpha,\beta)$-ruling set is a set of nodes that satisfies two properties:
\begin{itemize}
    \item Nodes in the set are at distance at least $\alpha$ from each other.
    \item Each node not in the set has a node in the set at distance at most $\beta$.
\end{itemize}
Observe that the problem of computing a $(2,1)$-ruling set is the same as MIS. We will now see how to encode $(2,2)$-ruling sets on $3$-regular graphs, in the node-edge formalism.
\begin{flalign*}
& \M^3 &\\
& \P \s \O^2 &\\
& \Q \s \V^2 &\\
\\
& \M \s \P\O &\\
& \O \s \Q &\\
& \O\V^2 &
\end{flalign*}
In other words:
\begin{itemize}
    \item Nodes in the set output $\M^3$, and $\M^2$ is not in the edge constraint, to satisfy independence.
    \item Nodes not in the set need to output \emph{pointers} to prove maximality. These pointers can be $\P$ or $\Q$. However, $\P$ is only compatible with $\M$, and hence nodes outputting $\P \s \O^2$ must be at distance $1$ from at least one node in the set. Nodes at distance $2$ from nodes in the set output $\Q \s \V^2$, and $\Q$ must point to an $\O$, and hence to a node at distance $1$, ensuring that all nodes outputting $\Q \s \V^2$ are at distance $2$ from nodes in the set. 
    \item Except for needing to output pointers, there are no further constraints, so on the edges we allow any choice over $\O\V^2$.
\end{itemize}
This is not the only way to encode $(2,2)$-ruling sets. We could have for example allowed $\M \s \Q\V$ on the edges, which would allow nodes outputting $\Q \s \V^2$ to be a distance $1$ from nodes in the set.
With both encodings, it would take $2$ rounds to convert a solution for (standard) $(2,2)$-ruling sets into the version encoded in the node-edge formalism.

If we were to generalize this idea to $(2,\beta)$-ruling sets, for larger $\beta$, we would essentially do it as follows:
\begin{itemize}
    \item We require nodes in the set to output $\M^3$.
    \item For each $1\le i\le \beta$, we would introduce two new labels $\P_i$ and $\O_i$.
    \item Then, nodes at distance $i$ from the set need to output $\P_i \s \O_i \s \O_i$, and $\P_i$ would need to point to $\O_{i-1}$ (or to $\M$, if $i=1$).
\end{itemize}
However, now observe that, in order to convert a $(2,\beta)$-ruling set into a solution of this encoded version, it would take $\beta$ rounds of communication, because nodes not in the set need to figure out their distance from the set.
We need to be very careful about this when proving lower bounds: suppose we use round elimination to prove an $f(\beta)$ lower bound for the version of the problem encoded in the node-edge formalism, for some function $f$. This would not give an $f(\beta)$ lower bound for $(2,\beta)$-ruling sets, but only an $f(\beta) - \beta$ lower bound. Depending on $f$, this could matter a lot.

\section{Problems vs Family of Problems}
Observe that all the considered examples were problems defined on $3$-regular graphs. In fact, we used sentences like ``let's see how to define, in the node-edge formalism, the problem of computing an MIS on $3$-regular graphs''. Observe that we never defined ``MIS'' or ``Maximal Matching'', we always only defined them on graphs of a fixed degree.
In fact, we could define (by considering a suitable generalization of the node-edge formalism) ``MIS on graphs of degree $\le 100$'', but we \emph{cannot} define MIS in the node-edge formalism. The reason is that there are infinite values of $\Delta$, but we want our problems to have finite description (so that we can apply round elimination on them).

The workaround is the following. We don't consider MIS to be a problem, but we consider it to be a \emph{family of problems}.
In other words, we define MIS (on regular graphs) as the family of problems, containing, for each $\Delta \in \mathbb{N}^+$, the following problem:
\begin{flalign*}
& \M^\Delta &\\
& \P \s \O^{\Delta-1} &\\
\\
& \M \s \O\P &\\
& \O \s \O &
\end{flalign*}
Yes, we just replaced $\Delta$ with a $3$.
However, think about defining, e.g., $\Delta$-coloring. Then it is not as easy as making some exponents depend on $\Delta$. We would now have that the number of configurations itself depends on $\Delta$, so we would need a new language for describing our problems.
This makes it hard to apply the round elimination technique: as we will see, this technique, as is, can be applied on a given problem, but if we want to apply it on an entire family of problems at once, things get more complicated. 
However, this also shows how we can step outside the LCLs world:
\begin{itemize}
    \item According to the notation that we just introduced, MIS is definitely \emph{not} an LCL. It is an infinite family of problems, one for each maximum degree $\Delta$.
    \item We use round elimination to prove, for each problem in the family, a lower bound.
    \item We see how the asymptotics goes, and we get lower bounds as a function of $\Delta$.
\end{itemize}

\chapter{Generalizations of the Black-White Formalism}
[TODO: two ways of define problems with inputs]
[TODO: non-regular graphs]

\chapter{Round Elimination}
Round elimination is a technique that can be used to prove lower (and upper) bounds in the LOCAL model. Informally, round elimination is a function $\re$ that takes as input a problem $\Pi$ in the black-white formalism and outputs a new problem $\Pi'$ in the black-white formalism, with the guarantee that $\Pi'$ is exactly one round easier than $\Pi$. This statement is omitting many details that we will see later. However, lower bounds are essentially obtained by applying this function multiple times.

\section{The function $\re$.}
Let $\Pi = (\Sigma,W,B)$. We now see how the problem $\Pi' = (\Sigma',B',W') = \re(\Pi)$ is formally defined. We will later then see some intuition and some examples.
First of all, as mentioned earlier, when describing a problem in the black-white formalism, we are implicitly specifying \emph{who} is going to output the labels. That is, while the problems $(\Sigma,W,B)$ and $(\Sigma,B,W)$ have exactly the same constraints, in the former case we require white nodes to output labels, and we say that white nodes are active, while black nodes are passive, while in the latter case we require the black nodes to output, and we say that black nodes are active and white nodes are passive. When applying the round elimination function, not only we obtain a problem that is exactly $1$ round easier, but we also flip the roles of active and passive. 
Let's now see the definition of $\Pi'$. Let $\Delta$ be the degree of the white nodes, and $\delta$ be the degree of the black nodes.

\begin{itemize}
    \item $\Sigma'$ is going to be a subset of $2^\Sigma \setminus \emptyset$.
    \item We first define $B'$ as a function of $B$. 
    \begin{itemize}
        \item We first define an auxiliary constraint $\ccs$ as the set of all configurations $\{L_1, \ldots, L_\delta\}$ such that, for all $1 \le i \le \delta$, it holds that $L_i \subseteq 2^\Sigma \setminus \emptyset$, and such that, for all choices $\ell_1 \in L_1, \ldots \ell_\delta \in L_\delta$, the configuration $\{\ell_1, \ldots, \ell_\delta\}$ is in $B$.
        \item We define $B'$ as the maximal configurations in $\ccs$, that is, the subset of $\ccs$ of configurations $\{L_1,\ldots,L_\delta\}$ such that there is no other configuration $\{L'_1,\ldots,L'_\delta\}$ in $\ccs$ such that there is a permutation $\phi$ of $\{1,\ldots,\delta\}$ such that:
        \begin{itemize}
            \item For all $1 \le i \le \delta$, $L_i \subseteq L'_i$;
            \item There is at least one $i \in \{1,\ldots,\delta\}$ such that $L_i \subsetneq L'_i$.
        \end{itemize}
    \end{itemize}
    \item $\Sigma'$ is defined as the set of labels that appear at least once in the configurations in $\ccs$, that is $\Sigma' = \bigcup_{\ccc \in \ccs} \bigcup_{L \in \ccc} \{L\}$.
    \item We now define $W'$ as a function of $W$ and $\Sigma'$. We define $W'$ as the set of all configurations $\{L_1, \ldots, L_\delta\}$ such that, for all $1 \le i \le \Delta$, it holds that $L_i \in \Sigma'$, and such that there exists a choice $\ell_1 \in L_1, \ldots \ell_\Delta \in L_\Delta$ such that the configuration $\{\ell_1, \ldots, \ell_\Delta\}$ is in $W$.
\end{itemize}

\section{Round Eliminator}
Given a problem $\Pi$, we do not have to manually compute $\re(\Pi)$. We can use a tool called \emph{round eliminator}, which can be found \href{\retor}{here}. There, you can input a problem in the black-white formalism, by writing the active constraint on the left textbox and the passive constraint on the right textbox, by using the same format used in this book. By pressing start, you get some info about the problem. 
By then pressing \retorcommand{Tools $\rightarrow$ Operations $\rightarrow$ Speedup}, you obtain $\re(\Pi)$. We will later see some other features of this tool.

\section{Some terminology}
We say that a configuration $L_1,\ldots,L_\delta$ \emph{satisfies the universal quantifier} if for all choices $\ell_1 \in L_1, \ldots \ell_\delta \in L_\delta$, the configuration $\{\ell_1, \ldots, \ell_\delta\}$ is in $B$. Hence we can say that $\ccs$ is the set of all configurations that satisfy the universal quantifier.
We say that a configuration is maximal if it is included in $B'$.
A configuration $L_1,\ldots,L_\delta$ is \emph{dominated} by $L'_1,\ldots,L'_\delta$ if there exists a permutation $\phi$ such that: for all $1 \le i \le \delta$, $L_i \subseteq L'_i$, and there is at least one $i$ for which the inclusion is strict. Note that $B'$ is obtained by removing dominated configurations from $\ccs$.

Similarly, we say that a configuration $L_1,\ldots,L_\delta$ \emph{satisfies the existential quantifier} if there exists a choice $\ell_1 \in L_1, \ldots \ell_\delta \in L_\delta$ such that the configuration $\{\ell_1, \ldots, \ell_\delta\}$ is in $W$.

We can now describe $\Pi'$ more compactly as follows:
\begin{itemize}
    \item $B'$ is the set of maximal configurations over the alphabet $2^{\Sigma} \setminus \emptyset$ that satisfy the universal quantifier.
    \item Let $\Sigma'$ be the set of labels appearing in $B'$.
    \item $W'$ is the set of configuration over $\Sigma'$ that satisfy the existential quantifier.
\end{itemize}

\section{An easy way to compute \texorpdfstring{$W'$}{W'}}
In order to compute $W'$, there is an easy way that one can follow, which is possible to prove equivalent. 
Start from $W'$ to be the empty set. Do the following operation in all possible ways. For each configuration in $W$, replace each label $\ell$ with a label from $\Sigma'$ that contains $\ell$. Add the resulting configuration to $W'$.

\section{Some intuition}
Let's unpack the definition and see one piece at a time.
It is also useful to see the proof of why $\Pi'$ is exactly one round easier than $\Pi$, to better understand why the definition is the one given.
First of all, if we defined $B' = \ccs$ (instead of taking only maximal configurations), the obtained problem would still be exactly one round easier. The only reason why we made the definition a bit more complicated is that it often makes the resulting problem ``smaller'', and hence easier to understand. So let's start by understanding $\ccs$. Think of the following:
\begin{itemize}
    \item We have an algorithm $\mathcal{A}$ that solves $\Pi$ in $T$ rounds, such that the output is produced by white nodes. We call this algorithm a \emph{white algorithm}.
    \item Recall that an algorithm in the LOCAL model is just a function that maps the view of a node (that is, its radius-$T$ neighborhood) into an output.
    \item Hence, a white algorithm does not have to be executed necessarily by a white node. If a black node $u$ gathers the view of a white node $v$, node $u$ can ask ``what would $v$ output?''
    \item We define a \emph{black algorithm} $\mathcal{A}'$ that takes $T-1$ rounds as follows:
    \begin{itemize}
        \item Each black node $u$ spends $T-1$ rounds to gather its radius-$(T-1)$ neighborhood $B_{T-1}(u)$.
        \item Observe that, if node $u$ gathered  $B_{T+1}(u)$ instead of $B_{T-1}(u)$, node $u$ could answer the following question for each white neighbor $v$: what would $v$ output on the edge $\{u,v\}$? This is doable because $B_{T+1}(u)$ contains $B_{T}(v)$, and $\mathcal{A}$, when running on $v$, solely depends on $B_{T}(v)$.
        \item We would like node $u$ to answer such a question, but node $u$ does not have enough information to answer it. However, node $u$ can answer the following question (See \Cref{fig:re-simulation}). Consider all possible ways of ``extending'' $B_{T-1}(u)$ such that $B_{T}(v)$ becomes fixed. For each such way, what is the output of $v$? By answering this question, node $u$ would obtain a set of outputs, and we can now try to infer some properties about such outputs. For now, this explains why the labels of $\Pi'$ are sets of labels of $\Pi$. 
    \end{itemize}
    \item Recall that node $u$ answered the question for each neighbor $v_1,\ldots,v_\delta$. So node $u$ now obtained $\delta$ sets $L_1, \ldots, L_\delta$, and we can ask, what do these sets satisfy? Could it be that there exists a choice $\ell_1 \in L_1, \ldots, \ell_\delta \in L_\delta$ such that $\{\ell_1,\ldots,\ell_\delta\} \notin B$? We will see that the answer is \emph{no}, and observe that $\ccs$ is defined as the constraint containing all configurations \emph{not} satisfying this property. In other words, we say, ``we don't know what these sets are going to satisfy, but they definitely do not satisfy this property, so let's take the \emph{easiest} problem not satisfying this property'' (recall that $\ccs$ contains \emph{allowed} configurations, so by putting \emph{all} configurations \emph{not} satisfying this property, we make the problem as \emph{easy} as possible). Suppose for a contradiction the answer is \emph{yes}. Then, we can take $B_{T-1}(u)$, and $\delta$ extensions of it, $Y_1, \ldots, Y_\delta$, and create a graph $G$ satisfying the following: when running $\mathcal{A}$, node $v_1$ outputs $\ell_1$, \ldots, node $v_\delta$ outputs $\ell_\delta$. We would obtain that $\mathcal{A}$ produces an invalid configuration on $u$, a contradiction. See \Cref{fig:re-contradiction}.
    \item Ignore for a moment that $\Pi'$ is defined by taking only maximal configurations from $\ccs$, and supposed we defined $\Pi'$ by using $\ccs$ directly. We obtained that $\Pi'$ can be solved in $T-1$ rounds by a black algorithm! This means that $\Pi'$ is \emph{at least one round easier} than $\Pi$.
    \item Can we also solve the \emph{real} $\Pi'$ in $T-1$ rounds, the one that does not allow non-maximal configurations? Actually, yes. Suppose node $u$, by computing $\{L_1,\ldots,L_\delta\}$, obtains a non-maximal configuration. Consider an arbitrary maximal configuration $\{L'_1,\ldots,L'_\delta\}$ satisfying that, for all $i$, $L_i \subseteq L'_i$. If $u$ outputs such a configuration, it is clearly still satisfying the black constraint. Moreover, since the white nodes need to satisfy an existential quantifier, and since we made each set larger, we obtain that the white constraint is also still satisfied.
    \item Let's now prove that $\Pi'$ is \emph{at most one round easier}. We prove this by showing that, given a black algorithm $\mathcal{A'}$ solving $\Pi'$ in $T-1$ rounds, we can create a white algorithm $\mathcal{A}$ solving $\Pi$ in $T$ rounds. The algorithm $\mathcal{A}$ simply works as follows. 
    \begin{itemize}
        \item Run $\mathcal{A'}$. This takes $T-1$ rounds.
        \item Each black node $u$ sends to each white neighbor $v$ the output that it produced for the edge $\{u,v\}$. This only takes $1$ round. 
        \item Each white node obtained sets $L_1,\ldots,L_\Delta$. By the definition of $W'$, white nodes can pick $\ell_1 \in L_1,\ldots,\ell_\Delta \in L_\Delta$ such that $\ell_1,\ldots,\ell_\Delta$ is in $W$, and output such a configuration. By the definition of $B'$, since the configurations in $B'$ satisfy the universal quantifier, all black nodes will be happy.
    \end{itemize}
\end{itemize}

\begin{figure}
\centering
\includegraphics[width=\textwidth]{figs/re-simulation.pdf}
\caption{Let $X$ be the red area. Let $Y$ be the purple area. Let $T = 3$. An algorithm running on $v$ would only depend on $X$ and $Y$ (note that the union of $X$ and $Y$ is exactly $B_3(v)$). Node $u$ only sees $B_{2}(u)$, which is exactly $X$. It does not see $Y$. Node $u$, in order to know the output of $v$, would need to know $Y$. Node $u$ considers all possible $Y$ (which means that it considers all possible ways in which that part of the graph could look like: all possible inputs, all possible random bit assignments, ...), and for each of them computes the output of node $v$.}
\label{fig:re-simulation}
\end{figure}

\begin{figure}
\centering
\includegraphics[width=\textwidth]{figs/re-contradiction.pdf}
\caption{Let $X$ be the red area. Let $Y_1$ be the purple area. Let $Y_2$ be the green area. Let $Y_3$ be the cyan area. Let $T = 3$. An algorithm running on $v_1$ would only depend on $X$ and $Y_1$ (note that the union of $X$ and $Y_1$ is exactly $B_3(v_1)$). An algorithm running on $v_2$ would only depend on $X$ and $Y_2$ (note that the union of $X$ and $Y_2$ is exactly $B_3(v_2)$). An algorithm running on $v_3$ would only depend on $X$ and $Y_3$ (note that the union of $X$ and $Y_3$ is exactly $B_3(v_3)$). 
Suppose there exist $Y_1, Y_2, Y_3$ such that $v_1,v_2,v_3$ output a configuration $\{\ell_1,\ell_2,\ell_3\} \notin \ccs$. We could create a graph in which $\mathcal{A}$ fails on $u$. This would contradict the correctness of $\mathcal{A}$.} 
\label{fig:re-contradiction}
\end{figure}

\section{Some technicalities}\label{sec:re-technicalities}
Recall that the core idea used for proving that $\Pi'$ is at least one round easier than $\Pi$ is the following:
\begin{itemize}
    \item Assume that, for each $i$, there is an extension $Y_i$ that, when combined it with $B_{T-1}(u)$, forces $v_i$ to output $\ell_i$.
    \item Then, we can create a graph $G$ that contains $B_{T-1}(u)$ and all $Y_i$ \emph{at the same time}. Recall \Cref{fig:re-contradiction}.
\end{itemize}
However, can we really do that? Imagine that the $Y_1$ that forces $v_1$ to output $\ell_1$ contains a node with id $123$, and that the $Y_2$ that forces $v_2$ to output $\ell_2$ contains also a node with id $123$. Then the graph $G$ that we create would not be a valid instance, because it would contain two distinct nodes with the same id.
This is the reason why round elimination does not directly apply to the LOCAL model, and some more machinery is needed.
As is, round elimination works in the so-called \emph{deterministic port-numbering model}, which is the same as deterministic LOCAL, but without IDs. This is a weak model, and we would not be satisfied if our lower bounds applied to just this model. There is another issue: suppose $T \gg \log_\Delta n$. For the graph to stay regular, in $T$ rounds of communication, we would necessarily have to see at least one cycle. This could also make $Y_1$ and $Y_1$ not independend of each other. This is why round elimination cannot give lower bounds that are asymptotically better than $\log_\Delta n$.

While how to get lower bounds better than $\log_\Delta n$ is still unknown, the other issue is fully solved. The route is the following:
\begin{itemize}
    \item We first make round elimination work in the \emph{randomized port-numbering model}, which is the same as randomized LOCAL but without IDs. In fact, one can prove that, if there is a white algorithm that solves $\Pi$ in $T$ rounds with failure probability $p$, then there is a black algorithm that solves $\Pi$ in $T-1$ rounds with failure probability $p'$ that is polynomially larger than $p$. This polynomial evolution in the failure probability is what makes randomized lower bounds obtained via round elimination asymptotically not better than $\log_\Delta \log n$.
    \item Then, we observe that randomized port-numbering and randomized LOCAL are essentially the same model: in the randomized port-numbering model we can generate unique ids with high probability. Thus, we obtain lower bounds for randomized LOCAL.
    \item Then, we use some existing techniques to convert a randomized lower bound into a lower bound for deterministic LOCAL that is even better.
    \item However, we really don't have to worry about all this! There are existing theorems that can be used fully as a black box, to convert a lower bound obtained via round elimination into lower bounds for the LOCAL model. We will discuss this in more detail later.
\end{itemize}

\section{An example: edge grabbing}\label{sec:re-edge-grabbing}
Edge grabbing requires each node to mark \emph{exactly one} incident edge, such that no edge is marked by both endpoints at the same time.
Observe that it is essentially the same problem as sinkless orientation:
\begin{itemize}
    \item Given a solution for sinkless orientation, each edge can mark an arbitrary outgoing edge.
    \item Given a solution for edge grabbing, nodes can label grabbed edges as \emph{outgoing}, and then they can spend $1$ round to know which of the incident edges are grabbed by the neighbors (and label them \emph{incoming}), and to agree on an orientation of the incident edges not grabbed by anybody. 
\end{itemize}
Hence, if we prove a lower bound of $T$ rounds for edge grabbing, we obtain a lower bound of $T$ rounds for sinkless orientation as well.
Let's consider this problem on $3$-regular graphs. The problem can be defined in the node-edge formalism as follows:
\begin{flalign*}
& \A \s\B^2 &\\
\\
& \B \s \A\B &
\end{flalign*}
In other words, $\A$ means \emph{grabbed}, and $\B$ means \emph{not grabbed}. Each node outputs exactly one $\A$. On the edges, at most one side can label the edge $\A$.
Recall that the notation above means that $\Pi = (\Sigma,W,B)$ is defined as:
\begin{itemize}
    \item $\Sigma = \{\A,\B\}$
    \item $W = \{\{\A,\B,\B\}\}$
    \item $B = \{\{\B,\A\},\{\B,\B\}\}$
\end{itemize}
We will compute $\Pi' = (\Sigma',B',W') = \re(\Pi)$. However, before that, please note the following. $\Pi'$ would be one round easier than $\Pi$ in a mode where we add a black node in the middle of each edge. But in the model that we are considering there are no such nodes. There are two (both correct) ways to interpret this:
\begin{itemize}
    \item We prove a lower bound of $T$ rounds in a model where such black nodes exist. By an easy simulation argument (distances in these two models differ by a multiplicative factor of $2$), this implies a lower bound of $T/2$ rounds in a model where such nodes do not exist.
    \item We are considering a model where edges can be computing entities, and we are saying that if we have a $T$-round algorithm for $\Pi$ running on nodes, we obtain an algorithm for $\Pi'$ in a model where edges are the computing entities and requires $T-\frac{1}{2}$ rounds. By applying $\re$ again, we will go back to a node algorithm that requires $T-1$ rounds.
\end{itemize}
It does not matter which interpretation we choose, if we manage to apply $\re$ for $x$ times, in both cases we get a lower bound of $x/2$ rounds for the case in which black nodes do not exist.

Let's compute $\Pi' = (\Sigma',B',W')$.
\begin{itemize}
    \item We compute $B'$. Let's first compute the configurations that satisfy the universal quantifier. We obtain the following.
\begin{flalign*}
& \{\B\} \s\{\B\} &\\
& \{\B\} \s\{\A\} &\\
& \{\B\} \s\{\A,\B\} &
\end{flalign*}
Indeed, there are exactly all the configurations over the labels $2^{\{\A,\B\}} \setminus \emptyset$ that contain $\A$ in at most one of the two sets (we need to satisfy this requirement because $B$ allows every configuration except for $\A \s \A$).
We now discard dominated (i.e., non-maximal) configurations. The only one remaining is $\{\B\} \s\{\A,\B\}$. Hence, $B'$ is the following constraint.
\begin{flalign*}
& \{\B\} \s\{\A,\B\} &
\end{flalign*}
    \item We obtain that $\Sigma' = \{\{\B\},\{\A,\B\}\}$.
    \item We now compute $W'$, which must contain all configurations from which we can pick one $\A$ and two $\B$. Any configuration different from $\{\B\}^3$ would satisfy this requirement. Hence, $W'$ contains the following configurations:
\begin{flalign*}
& \{\A,\B\} \s \{\B\}^2 &\\
& \{\A,\B\}^2 \s \{\B\} &\\
& \{\A,\B\}^3 &
\end{flalign*}
\end{itemize}
We computed $\Pi'$. However, let us make it easier to read. Let's rename some labels. Let use replace $\{\B\}$ with $\A$ and $\{\A,\B\}$ with $\B$. We obtain that $\Pi'$ is the following problem:
\begin{flalign*}
& \A \s\B &\\
\\
& \B \s \A^2 &\\
& \B^2 \s \A &\\
& \B^3 &
\end{flalign*}
We can write this problem more compactly as:
\begin{flalign*}
& \A \s\B &\\
\\
& \B \s \A\B^2 &
\end{flalign*}
Let's now compute $\Pi'' = (\Sigma'',W'',B'') = \re(\Pi')$.
\begin{itemize}
    \item We first compute $W''$. By the definition of $W'$, we want all configurations over $2^{\Sigma'} \setminus \emptyset$ from which we can pick at least one $\B$. If we consider only the maximal ones, we get that the only configuration contained in $W''$ is the following:
\begin{flalign*}
& \{\B\} \s \{\A,\B\}^2 &
\end{flalign*}
    \item Let's now compute $B''$ by using the easy method. We start from $\A \s \B$ and we replace $\A$ with all sets containing $\A$ (which is only $\{\A,\B\}$) and we replace $\B$ with all sets containing $\B$ (which are $\{\B\}$ and $\{\A,\B\}$). We obtain the following condensed configuration:
\begin{flalign*}
& \{\A,\B\} \s \{\B\}\{\A,\B\} &
\end{flalign*} 
\end{itemize}
As before, let us rename the labels to make the problem easier to read. Let's rename some labels. Let use replace $\{\B\}$ with $\A$ and $\{\A,\B\}$ with $\B$. We obtain that $\Pi''$ is the following problem:
\begin{flalign*}
& \A \s\B^2 &\\
\\
& \B \s \A\B &
\end{flalign*}
We obtained that $\Pi'' = \Pi$. There must be something wrong. How can $\Pi$ be exactly $2$ rounds easier than itself?
There is nothing wrong: remember the small technicalities mentioned in 
\Cref{sec:re-technicalities}. We can interpret what we obtained as follows:
\begin{framed}
Suppose $\Pi$ can be solved in $T$ rounds, such that $T$ is small-enough so that the requirements of round elimination apply (that is, there are no cycles in the radius-$T$ neighborhood of the nodes). Then, $\Pi$ is $2$ rounds easier than itself. Since the conclusion is a contradiction, the premise is false, hence $\Pi$ cannot be solved with small-enough $T$.
\end{framed}

As said earlier, there are some black-box theorems that we can apply. One of them is the following:
\begin{theorem}\label{thm:basic-lifting-fp}
Suppose that $\Pi = \re(\Pi)$ or $\Pi = \re(\re(\Pi))$. Then, $\Pi$ requires $\Omega(\log n)$ for deterministic algorithms in the LOCAL model, and $\Omega(\log \log n)$ for randomized ones.
\end{theorem}
We thus get that edge grabbing, and sinkless orientation, in $3$-regular graphs, have deterministic and randomized lower bounds of $\Omega(\log n)$ and  $\Omega(\log \log n)$ respectively. As a side note, such lower bounds (and all lower bounds obtained via round elimination) also apply to regular balanced trees as well. Another note: the $\Omega$ notation in \Cref{thm:basic-lifting-fp} is hiding dependencies in $\Delta$ and $\delta$. While for our specific case it does not matter (because we are considering the problem on $3$-regular graphs), suppose we tried to prove a lower bound for edge grabbing in $\Delta$-regular graphs as a function of $n$ and $\Delta$. In other words, suppose we tried to prove a lower bound for a whole family of problems at the same time. Then we would have needed to use a different lifting theorem, that correctly takes $\Delta$ into account.

\section{Exercise: Edge Grabbing on Round Eliminator}
Try to replicate \Cref{sec:re-edge-grabbing} on \href{\retor}{Round Eliminator}.

\paragraph{Step 1: input the problem.}
Write \verb|A B^2| on the left textbox (\retorcommand{Active}), write \verb|B AB| on the right textbox (\retorcommand{Passive}).

\paragraph{Step 2: get some info.}
Press \retorcommand{Start}. Round eliminator gives some basic info about the problem. E.g., it says that it cannot be solved in $0$ rounds. We will later discuss how this is detected.

\paragraph{\boldmath Step 3: compute $\re(\Pi)$.}
Press \retorcommand{Tools $\rightarrow$ Operations $\rightarrow$ Speedup}.
By default, the obtained problem has its labels already renamed (by default, sets are ordered in some way, and then renamed by using alphabet letters). In the \retorcommand{Renaming} tab, we can see that $\A$ corresponds to the set $\{\B\}$ and that $\B$ corresponds to the set $\{\A,\B\}$, which is denoted as $\mroundbox{\A\B}$ in Round Eliminator.
If we want to see the configurations before the renaming, we can press \retorcommand{Old}. By pressing \retorcommand{Both}, we see the configurations before and after the renaming at the same time.
We do not see if the obtained problem can be solved in $0$ rounds: by default, this is computed only when the passive constraint has degree $2$ (because it is otherwise heavy to compute). We could see that the obtained problem is not $0$-round solvable by pressing \retorcommand{Tools $\rightarrow$ Operations $\rightarrow$ Maximize} (we will later discuss what this does), but let us ignore this step for now.

\paragraph{\boldmath Step 4: compute $\re(\re(\Pi))$.}
Again, press \retorcommand{Tools $\rightarrow$ Operations $\rightarrow$ Speedup}. We see that the obtained problem is equal to $\Pi$.
You can see the whole result \href{\retor#}{here}.

\section{Fixed Points}
What we have seen in \Cref{sec:re-edge-grabbing} is an example of so-called \emph{fixed point}, and in particular a \emph{non-trivial} fixed point.
Non-trivial here just means that the fixed point is not solvable in $0$ rounds of communication. I.e., the problem 
\begin{flalign*}
& \A^3 &\\
\\
& \A^2 &
\end{flalign*}
is also a fixed point, but it is a trivial problem: everybody can output $\A$ on all its incident edges.

As previously discussed, LCL problems can be classified as being \emph{easy} or \emph{hard}. In order to prove that a problem is easy, it is sufficient to provide an $O(\log^\star n)$ algorithm. In order to prove that a problem is hard, we need to provide a deterministic  $\Omega(\log n)$ lower bound.
As discussed, showing that a problem is a non-trivial fixed point implies that the problem is $\Omega(\log n)$. However, very few interesting problem are non-trivial fixed points.
A technique that we often use to prove an $\Omega(\log n)$ lower bound is to provide a non-trivial fixed point \emph{relaxation}, that is, given a problem $\Pi$ for which we want to prove a lower bound, we find a problem $\Pi'$ that is \emph{at least as easy as} $\Pi$ and such that $\Pi'$ is a non-trivial fixed point.
Providing a fixed point relaxation for a problem is one of the very few know ways that we know to prove that the problem is $\Omega(\log n)$.
A major open question is the following:
\begin{oq}\label{oq:fp}
    Does every $\Omega(\log n)$ problem admit a non-trivial fixed point relaxation?
\end{oq}
In other words, we don't know if, providing a non-trivial fixed point relaxation for a problem, is a universal way to prove lower bounds.
Any answer to \Cref{oq:fp} would have important consequences:
\begin{itemize}
    \item If the answer is ``yes'', it would imply \emph{decidability}. In more detail, it is known that it is semi-decidable whether a problem is solvable in $O(\log^\star n)$ rounds. Since it is semi-decidable whether a problem admits a non-trivial fixed point relaxation, if the answer is ``yes'' then we would get that it is semi-decidable whether the problem is $\Omega(\log n)$. Combining the two, we would get that we can decide if a problem is easy or hard.
    \item If the answer is ``no'', then it means that there are problems that are $\Omega(\log n)$ for which we have no known ways to prove that they are indeed $\Omega(\log n)$.
\end{itemize}
Currently, we have \emph{zero} problems that we know to be $\Omega(\log n)$ but for which we do not know a non-trivial fixed point relaxation.

In the past, we had some. There is a technique that, in the distributed setting, is called Mark's technique, and comes from a different field called \emph{descriptive combinatorics}. This technique can be used to prove that a problem does not belong to a class of problems called \emph{Borel}. It is known that if a problem is not in this class, then the problem requires $\Omega(\log n)$ (note, it is not an if and only if). For quite a while we knew of a class of problems called \emph{homomorphism problems}, for which Mark's technique succeeds in giving lower bounds.
These problems were candidates for answering ``no'' to \Cref{oq:fp}. We now know that, if Mark's technique succeeds for a problem, then we can always construct a non-trivial fixed point relaxation for the problem.
So currently we have zero candidates that could help in answering ``no'' to the question, and we know that such candidates cannot be obtained via Mark's technique.
However, we know that the answer \emph{is} ``no'' for LCLs with inputs. In more detail, consider the problem of computing a sinkless and sourceless orientation (SSO) given as input a solution for sinkless orientation (SO), that we will call SSO-given-SO.
We know a non-trivial fixed point relaxation for SSO (which is edge grabbing, EG). However, EG is a trivial problem in the context in which a solution for SO is given as input. It has been proved that \emph{any} non-trivial fixed point relaxation of SSO \emph{is trivial} given a solution for SO. So how do we know that SSO-given-SO is $\Omega(\log n)$? Well, providing a fixed point is not really the only way to prove such a lower bound:
\begin{itemize}
    \item SSO behaves very nicely under round elimination, the number of labels grows by $1$ at each $\re$ step.
    \item There are lifting theorems that say that if we have an infinite sequence of problems given by $\re$ that are all non-trivial, where the number of labels grows ``slow-enough'', then the initial problem is $\Omega(\log n)$.
\end{itemize}
But SSO is really an exception, the typical behavior of an interesting problem $\Pi$ is the following:
\begin{itemize}
    \item If we try to apply round elimination to $\Pi$, we see that, at each step, the number of labels grows exponentially.
    \item We try to \emph{relax} $\Pi$, or $\re(\Pi)$, or $\re(\re(\Pi))$, ..., with the hope to obtain a non-trivial fixed point.
    \item In all known cases, if we reach a problem that behaves as nicely as SSO (i.e., linear growth in the number of labels), then we can relax the problem a bit more and get a non-trivial fixed point.
\end{itemize}
So the only way this idea of ``we can't get a fixed point but linear growth is enough for a lower bound'' has been used is to prove a lower bound in the setting with input.
In fact, a related open question is the following:
\begin{oq}
    Is there a universal technique that can be used to prove lower bound in the setting of LCLs with input?
\end{oq}
We know that ``fixed points'' are not the answer, because of SSO-given-SO. And it seems unlikely that ``linear growth'' is the answer. An example of problem for which we know how to get lower bounds via reductions but not directly via round elimination is, e.g., the problem $\Pi$ of computing a $4$-coloring in $4$-regular graphs, given an orientation where every node has exactly $2$ incoming and $2$ outgoing edges.
For this problem, we do not know whether it is possible to define a sequence of problems $\Pi_1, \ldots,$ such that $\Pi_1 = \Pi$ and $\Pi_{i+1}$ is a relaxation of $\re(\Pi_i)$ and such that the growth in the number of labels is not exponential.

For now, we will put the topic of inputs aside, and we will discuss fixed points in the setting with no input. We will see some examples of fixed points and of non-trivial fixed point relaxations.


\subsection{Example: 2-coloring}
Consider the problem of $2$-color a $3$-regular graph. In the node-edge formalism, it can be defined as follows:
\begin{flalign*}
& \A^3 &\\
& \B^3 &\\
\\
& \A \s \B &
\end{flalign*}
An easy way to see that it is a fixed point is the following: if we pick any pair of configurations from the node side (including different permutations of the same configuration), or any pair on the edge side, the edit distance between them is $\ge 2$.
This implies that, when we write down the configurations satisfying the universal quantifier, we only get the original configurations, where we replace labels with singleton sets. This ensures that the problems does not change when applying round elimination, and after two steps of $\re$ we get back the problem itself.

\paragraph{Exercise.} Input the problem on \href{\retor}{Round Eliminator}. Press \retorcommand{Speedup} twice, see that you get back the same problem. Solution is \href{\retor#}{here}.

\subsection{Example: perfect matching}
Consider the problem of computing a perfect matching in a $3$-regular graph. In the node-edge formalism, it can be defined as follows:
\begin{flalign*}
& \M \s \O^2 &\\
\\
& \M \s \M &\\
& \O \s \O &
\end{flalign*}
In other words, each node marks exactly one edge, and an edge must be either marked by both endpoints or by none of them.
For the same reason as $2$-coloring, this problem is a fixed point.

Problems that satisfy the aforementioned edit-distance $\ge 2$ property are typically only problems that are so hard that one could prove lower bounds via different techniques. The more typical case is that the problem of interest is not a fixed point, and we need to find a non-trivial fixed point relaxation for it.

\paragraph{Exercise.} Input the problem on \href{\retor}{Round Eliminator}. Press \retorcommand{Speedup} twice, see that you get back the same problem (up to renaming). Solution is \href{\retor#}{here}.


\subsection{Example: sinkless orientation}
Sinkless orientation on $3$-regular graphs can be defined in the node-edge formalism as follows:
\begin{flalign*}
& \A \s \A\B^2 &\\
\\
& \A \s \B &
\end{flalign*}
Note that this problem does not satisfy the edit-distance $\ge 2$ property: on the nodes, we could pick $\A^3$ and $\A^2 \s \B$, which have edit distance $1$.
One can also check that this problem is not a fixed point. In fact, one can check that  $\re(\re(\mathrm{SO})) = \mathrm{EG}$, and note that $\mathrm{EG} \neq \mathrm{SO}$.
But we have already seen that EG is a fixed point in \Cref{sec:re-edge-grabbing}.
Since $\re(\re(\mathrm{SO}))$ is a relaxation of SO (the $\re$ operator guarantees that), we get that EG is a fixed point relaxation of SO. One can also check that EG is a non-trivial problem (we will later see how), and we hence get that EG is a non-trivial fixed point relaxation of SO.
Unfortunately, this is really an exceptional case, problems do not tend to naturally become fixed points after few steps of $\re$.

\paragraph{Exercise.} Input the problem on \href{\retor}{Round Eliminator}. Press \retorcommand{Speedup} \emph{four times}, see that, after two steps, we obtain EG, and after two other steps, we obtain EG again. Solution is \href{\retor#}{here}.


\subsection{Example: sinkless and sourceless orientation}
Sinkless and sourceless orientation on $3$-regular graphs can be defined in the node-edge formalism as follows:
\begin{flalign*}
& \A \s \B \s \A\B &\\
\\
& \A \s \B &
\end{flalign*}
That is, each edge must be properly oriented, and each node needs to have at least one incoming and at least one outgoing edge.
This problem is not a fixed point, and one can check that this problem would not converge into a fixed point by applying $\re$ (the number of labels would grow by $1$ at each step). 
However, there is a really easy relaxation that we can perform: we add $\A^3$ to the allowed node configurations. We get that the node constraint is 
\begin{flalign*}
& \A \s \B \s \A\B &\\
& \A^3 &
\end{flalign*}
which can be also written as 
\begin{flalign*}
& \A \s \A\B^2 &
\end{flalign*}
which is the same node constraint as sinkless orientation.
In other words, we can relax SSO to SO, and SO to EG, and EG is a non-trivial fixed point. We get that SSO admits a non-trivial fixed point relaxation.

\paragraph{Exercise.} Input the problem on \href{\retor}{Round Eliminator}. Press \retorcommand{Speedup} some times, and see that at each step we get one more label. Solution is \href{\retor#}{here}.

\subsection{Example: 3-coloring}
Consider the problem of $3$-coloring $3$-regular graphs. It can be defined as follows:
\begin{flalign*}
& \A^3 &\\
& \B^3 &\\
& \C^3 &\\
\\
& \A \s \B\C &\\
& \B \s \C &
\end{flalign*}
This is an example of a problem for which, finding a non-trivial fixed point relaxation, is not entirely trivial. In fact, we knew that this problem was hard even before knowing a fixed point for it, and it was proved as follows:
\begin{itemize}
    \item Consider the setting in which the graph comes with a $3$-edge coloring given.
    \item One can prove that, in such a setting, EG is still non-trivial. Not only, one can also prove that, in such a setting, EG is still \emph{hard}.
    \item However, given a $3$-coloring \emph{and} a $3$-edge coloring, EG becomes $0$-round solvable (nodes of color $x$ grab the edge with color $x$).
    \item Moreover, if we are only given a $3$-coloring, \emph{but not} a $3$-edge coloring, then EG is still non-trivial.
    \item This suffices to infer that $3$-coloring is a hard problem, that is, it requires $\Omega(\log n)$ deterministic rounds. In fact, suppose for a contradiction that $3$-coloring was easy. We would get that, given a $3$-edge coloring, we could solve an easy problem ($3$-coloring) and then use the obtained solution to solve EG, but we know that EG is hard given a $3$-edge coloring.
\end{itemize}
A non-trivial fixed point relaxation for $3$-coloring came much later. A possible reason is the following: if we take the natural generalization of this problem, that is, $\Delta$-coloring $\Delta$-regular graphs, the ``smallest'' non-trivial fixed point relaxation that we know is a problem that requires $2^\Delta$ labels to be described. So, in some sense, to get a fixed point we needed to guess a problem that has exponentially larger description size.

A non-trivial fixed point relaxation for $3$-coloring is the following. We use some new notation here. $(\A\B)$ represents a single label. That is, if we want to give a name to a label and use multiple characters, we put those character between parentheses.
\begin{flalign*}
& \A^3 &\\
& \B^3 &\\
& \C^3 &\\
& (\A\B)^2 \s \E &\\
& (\A\C)^2 \s \E &\\
& (\B\C)^2 \s \E &\\
& (\A\B\C) \s \E^2 &\\
\\
& \E \s \E\A\B\C(\A\B)(\A\C)(\B\C)(\A\B\C) &\\
& \A \s \E\B\C(\B\C) &\\
& \B \s \E\A\C(\A\C) &\\
& \C \s \E\A\B(\A\B) &\\
& (\A\B) \s \E\C &\\
& (\A\C) \s \E\B &\\
& (\B\C) \s \E\A &\\
& (\A\B\C) \s \E&
\end{flalign*}
Note that some configurations have been written multiple times, just to more easily see the pattern. Think of the label $\E$ as ``empty'', or as ``no color'', and more generally think of each label as a set of colors. For example $\A$ is the set containing only color $\A$, while $(\A\B)$ is the set containing colors $\A$ and $\B$, while $\E$ is the empty set.
We can see that the problem allows the following solutions:
\begin{itemize}
    \item Nodes can output a color, as in the case of $3$-coloring, or
    \item Nodes can output multiple colors. However, if they output $k$ colors, then they can output the empty set on $k-1$ incident half-edges. For example, if a node outputs $\A\B^2 \s \E$, then we can interptet it as being colored $\A$ and $\B$ at the same time.
    \item The edge constraint requires the endpoints of an edge to have assigned disjoint sets.
\end{itemize}
In other words, this is a generalization of coloring. In standard coloring, each node outputs exactly one color. In this version, nodes may output multiple colors. Note that outputting multiple colors is a hard task: if a node outputs $(\A\B)$ on an edge, the other endpoint cannot output neither $\A$ nor $\B$ on the same edge. However, nodes that output multiple colors are rewarded: for each additional color that they output (except for the first), they can mark an incident edge as ``don't care''.
One can prove that this problem is a fixed point and is not trivial.

Summarizing, $3$-coloring is not a fixed point, but we can find a non-trivial fixed point relaxation of it that requires $8$ labels to be described, and this fixed point has a natural interpretation.


\subsection{Example: locally optimal cut}
Not all fixed points admit a natural interpretation. We will now discuss a problem for which proving an $\Omega(\log n)$ lower bound has been significantly harder.
The problem is the one of computing a locally optimal cut (LOC) in $3$-regular graphs. A locally optimal cut is a $2$-coloring of the nodes satisfying that, if a node has a color, it needs to have at least two neighbors of the opposite color. It is a locally optimal cut in a game-theoretic sense: if nodes try to selfishly maximize the number of neighbors of the opposite color by unilaterally deciding to change color, then this problem is a Nash-stable solution. This problem is also called $1$-defective $2$-coloring: it is a $2$-coloring where nodes are allowed to have at most $1$ neighbor of the same color. This problem is also called unfriendly partition or majority coloring.
In the node-edge formalism, this problem can be encoded as follows.
\begin{flalign*}
& \A^2 \s \X &\\
& \B^2 \s \Y &\\
\\
& \A\X \s \B\Y &\\
& \X\Y \s \X\Y &
\end{flalign*}
That is, nodes of one color output $\A^2 \s \X$, while nodes of the other color output $\B^2 \s Y$. The labels $\X$ and $\Y$ are used to mark the edge in which there could be a neighbor of the opposite color. In fact, label $\X$ is compatible with $\X$ (so two neighbors of the first color can both label their connecting edge with $\X$) but not with $\A$ (as otherwise a node of the first color could end up having more than $1$ neighbor of the first color), and is also compatible with both $\B$ and $\Y$ (because they are both labels used by nodes of the opposite color).

The original proof showing that this problem is $\Omega(\log n)$ uses a reduction from the hardness of sinkless orientation. This reduction is non-trivial. In fact, we cannot directly convert a solution for LOC into a solution for SO, and the proof follows a different route. The proof explots the fact that, if a problem can be solved in $o(\log n)$, then it can also be solved in $O(\log^\star n)$ in a normal form. The normal form proceeds as follows:
\begin{itemize}
    \item For some suitable parameters $c$ and $d$, compute a distance-$d$ $c$-coloring, that is, a coloring with $c$ colors such that nodes within distance $d$ have different colors.
    \item Apply an $O(1)$-round algorithm after that.
\end{itemize} 
The lower bound is obtained by showing that, if we have an $o(\log n)$ rounds algorithm for LOC, and we normalize it in this way, then we can construct a suitable virtual graph where we run this algorithm, and we can convert the obtained solution into a sinkless orientation solution on the original graph.

For quite a white we did not have an alternative proof of hardness, and this problem was a candidate to answer ``no'' to \Cref{oq:fp}. However, we eventually got a fixed point for this problem, which is the following:
\begin{flalign*}
& (\A\X)^2 \s \X &\\
& (\B\Y)^2 \s \Y &\\
& (\A\B\X\Y\p) \s (\X\Y) \s \emptyset&\\
& (\B\X\Y+) \s (\A\X\Y\p) \s \emptyset&\\
& (\A\X\Y\p) \s \X^2 &\\
& (\B\X\Y\p) \s \Y^2 &\\
& (\X\Y\p) \s (\X\Y)^2 &\\
\\
& \X(\A\X)\emptyset \s (\B\Y)\emptyset \Y &\\
&\emptyset \s \X(\A\X)(\B\Y)(\X\Y)(\X\Y\p)\emptyset(\B\X\Y\p)(\A\B\X\Y\p)(\A\X\Y\p)\Y &\\
&\X\emptyset \s \X(\B\Y)(\X\Y)(\X\Y\p)\emptyset(\B\X\Y\p)\Y &\\
&\X(\A\X)(\X\Y)(\X\Y\p)\emptyset(\A\X\Y\p)\Y \s \emptyset \Y &\\
&\X(\X\Y)\emptyset \Y \s \X(\X\Y)(\X\Y\p)\emptyset \Y &
\end{flalign*}
This is a relaxation of the original problem: if we rename $(\A\X)$ to $\A$ and $(\B\Y)$ to $\B$, we get that the allowed configurations of LOC are allowed also here. However, differently from the case of the fixed point relaxation of $3$-coloring, it is quite hard to get some intuition about this problem. Does it have some natural interpretation? We don't know.
This fixed point was found by hand, by manually trying to relax the problems obtained by applying $\re$ on LOC. However, by carefully analyzing this fixed point, one can actually find some very hidden pattern. In fact, there is a mechanical way that one can use to obtained this fixed point by starting from LOC. We will later discuss this mechanical procedure. The same procedure, applied to $3$-coloring, gives the fixed point relaxation of $3$-coloring that we have seen earlier. In fact, all known fixed points can be obtained by applying this procedure, and discovering this procedure allowed us to find non-trivial fixed point relaxations that are so complicated (hundreds of labels) that finding them manually would have been hopeless.


\subsection{Exercise: getting fixed points automatically.}
Input the $3$-coloring problem on \href{\retor}{Round Eliminator}. As mentioned, there is a mechanical way to obtain a non-trivial fixed point relaxation for it. Round Eliminator implements this procedure, and it can be used by pressing  \retorcommand{Tools $\rightarrow$ Fixed Point Tools $\rightarrow$ Basic}. Try it, see that the obtained fixed point is the same as the one previously discussed. Solution is \href{\retor/#}{here}.

Now try the same on the LOC problem (solution \href{\retor/#}{here}). What do you notice?
Yes, the obtained problem is solvable in $0$ rounds. So the mentioned procedure does not always succeed, even for problems that admit a non-trivial fixed point relaxation. There is a way to tweak the procedure so that it works on LOC (we will discuss this procedure in more detail later). Start again from LOC. This time press \retorcommand{Tools $\rightarrow$ Fixed Point Tools $\rightarrow$ Generate Default Diagram}. Then, scroll down, and on the obtained problem, in \retorcommand{Tools $\rightarrow$ Fixed Point Tools $\rightarrow$ With Label Duplication}, click on label $(\X\Y)$ and then press \retorcommand{Add from diagram selection}. Then press \retorcommand{Generate Fixed Point}. We now obtained (up to renaming) the same fixed point discussed earlier (solution \href{\retor/#}{here}). 



\section{Relaxations}
We now put the discussion about fixed points aside, and we go back to the basics. We are given a problem $\Pi$, how can we try to construct a problem sequence that gives a lower bound? If we compute $\re(\Pi)$, and then $\re(\re(\Pi))$ and so on, for most of the problems of interest we quickly get so many labels that it is hard to find a natural interpretation of the problem, or to even compute the next problem. The basic goal to apply round elimination successfully is to manage to do the following. 
We want to replace $\re(\Pi)$ with some problem $\Pi'$ such that:
\begin{enumerate}
    \item $\Pi'$ must be at least as easy as $\Pi$.
    \item $\Pi'$ has much smaller description size (number of labels, number of configurations, ...) compared to $\re(\Pi)$.
    \item $\Pi'$ must not be \emph{too easy} compared to $\re(\Pi)$.
\end{enumerate}
We want to do this multiple times, and construct a sequence $(\Pi_i)_{i\ge1}$ satisfying the following:
\begin{itemize}
    \item $\Pi = \Pi_1$
    \item $\Pi_{i+1}$ is at least as easy as $\re(\Pi_i)$
    \item Let $k$ be the largest $k$ such that $\Pi_k$ is non-trivial (not $0$-rounds solvable). We obtain that $\Pi$ requires at least $k$ rounds.
\end{itemize}
Observe that requirement 1 is needed so that the non-triviality of $\Pi_k$ indeed implies a lower bound for $\Pi$. Requirement $3$ is needed because, if for some $i$ $\Pi_{i+1}$ is much easier than $\re(\Pi_i)$, then we cannot hope to get a tight lower bound. In fact, imagine the real complexity of $\Pi$ to be $T$, and suppose that $\Pi_2$ is $T-5$ rounds easier than $\re(\Pi)$. Then we cannot hope to get a lower bound which is better than $5$. But maybe $T$ was much larger than $5$.
Requirement 2 is needed to keep the whole operation tractable. In fact, we typically have a stronger goal than just keep the problems small. The \emph{real} goal is to be able to describe the whole sequence $\Pi_1,\ldots$ in a closed form. An example of a convenient closed form is a problem where some exponents depend on the index $i$ of the problem. We will see some examples soon. Before that, we first need to discuss how we can actually make a problem \emph{easier and smaller} at the same time.
In fact, there is a simple way to make a problem smaller: just discard some labels, and all configurations containing those labels. Unfortunately, this would make a problem \emph{harder}, not easier, because we are removing allowed configurations. However, there is something similar that we can do, that makes a problem \emph{easier}: merging labels.
Let us see an example with LOC, and in particular we see what it means to merge $\X$ and $\Y$. We modify the problem so that, every time $\X$ is allowed, we allow also label $\Y$, and vice versa. We obtain the following problem $\Pi'$.
\begin{flalign*}
& \A^2 \s \X\Y &\\
& \B^2 \s \X\Y &\\
\\
& \A\X\Y \s \B\X\Y &\\
& \X\Y \s \X\Y &
\end{flalign*}
In a solution for this problem, we can always replace $\X$ with $\Y$ and vice versa (e.g., if a node uses the configuration $\A^2 \s \X$, and we change it to $\A^2 \s \Y$, we still obtain a valid solution). Hence, the following problem $\Pi''$ has the exact same complexity:
\begin{flalign*}
& \A^2 \s \X &\\
& \B^2 \s \X &\\
\\
& \A\X \s \B\X &
\end{flalign*}
In fact, a solution for $\Pi''$ is also a solution for $\Pi'$, and a solution for $\Pi'$ can be converted in $0$ rounds of communication into a solution for $\Pi''$, by just replacing every $\Y$ with $\X$.
The obtained problem $\Pi''$ is called $1$-arbdefective $2$-coloring, and it turns out to be solvable in just $1$ round. Suppose we were trying to prove a lower bound for LOC, and suppose that, in order to keep things simpler, we replaced LOC with $\Pi''$, which is a relaxation of LOC. Since $\Pi''$ is solvable in $1$ round, it would have not been possible to obtain a non-trivial fixed point starting from $\Pi''$.

\begin{framed}
In a very simplified way, the act of finding lower bounds via round elimination boils down to find the right relaxations so that we obtain a sequence of problems that are very similar to each other, so that we can describe all of them as a single problem parameterized by some parameter $i$.
\end{framed}

Is there a way to detect if a relaxation is good, or do we have to try all possible relaxations by brute force? We will discuss this later. Before that, we will discuss some useful ingredients.

\paragraph{\boldmath Exercise: merging $\X$ and $\Y$.}
Input the LOC problem on \href{\retor}{Round Eliminator}. 
Then, relax the problem by merging $\X$ and $\Y$:
\begin{itemize}
 \item Press \retorcommand{Tools $\rightarrow$ Simplify}
 \item Choose label $\Y$ as \retorcommand{From}
 \item Choose label $\X$ as \retorcommand{To}
 \item Press \retorcommand{Merge}
\end{itemize}
Press \retorcommand{Speedup} twice. Observe that the obtained problem is $0$-round solvable (solution \href{\retor/#}{here}).


\paragraph{\boldmath Exercise: $3$-coloring fixed point.}
We have seen that finding a non-trivial fixed point relaxation for $3$-coloring is easy by using an automatic procedure. 
Historically, the fixed point for $3$-coloring was found before the discovery of this procedure, by just merging labels. Try to find a non-trivial fixed point in this way. This exercise is non-trivial. However, it becomes easy by following this idea:
\begin{itemize}
    \item We can ask Round Eliminator to use a different strategy for renaming of the labels, that renames the obtained labels as a function of the labels that compose them. This feature can be found at \retorcommand{Tools $\rightarrow$ Operations $\rightarrow$ Rename by generators}.
    \item Start from $3$-coloring, then repeat the following: press \retorcommand{Speedup} and on the obtained problem press \retorcommand{Rename by generators}. 
    \item After this renaming, the set $\{\A,\B\}$ is now renamed as $\langle \A, \B \rangle$.
    \item By doing this operation twice, we obtain things that look like the following: $\langle\langle\A,\B\rangle\rangle$ and $\langle\langle\A\rangle,\langle\B\rangle\rangle$. Interpret the former as ``$\A$ \emph{and} $\B$'' and the latter as ``$\A$ \emph{or} $\B$''.
    \item Try the following merging strategy:  merge ``or labels'' toghether, then rename in some arbitrary way so that $\langle$ and $\rangle$ symbols disappear, by using \retorcommand{Tools $\rightarrow$ New Renaming}, then repeat. You will eventually get a non-trivial fixed point. In order to merge more than $2$ labels together, you can either use the already mentioned \retorcommand{Simplify} feature multiple times, or you can use the \retorcommand{Group Simplify} feature to do the same in a single step.
\end{itemize}
The solution can be found \href{\retor/#}{here}.
This exercise shows why it took so much to get a non-trivial fixed point relaxation for $\Delta$-coloring: we had to essentially guess a good strategy. Try to play with $3$-coloring: try different relaxations, try to not perform relaxations at all. You will notice that one of the following will happen:
\begin{itemize}
    \item The problem becomes $0$ rounds solvable.
    \item The problem becomes solvable with $4$ or more colors. Note that $4$-coloring $3$-regular graphs is \emph{easy}, i.e., it can be done in $O(\log^\star n)$ rounds. Hence if we get a problem solvable with $4$ colors it means that it is not possible to apply $\re$ and perform mergings of labels to get a non-trivial fixed point.
    \item The problem becomes too large. You can see that if you do not perform any relaxation at all, the number of labels grows exponentially at each step.
\end{itemize}
However, ``merging \emph{or labels}'' is a strategy that works for many other problems. An example in which it fails is LOC.

\subsection{Zero-round Solvability}
A zero-round algorithm is just a mapping from a node's initial knowledge to a configuration. In the LOCAL model, the initial knowledge of a node includes its own ID, which means that different nodes may output different configurations. However, as discussed, when using round elimination, we first prove a lower bound in the deterministic port numbering model, and then we lift it to the LOCAL model via some lifting theorems. This helps a lot in determining when a problem can be solved in zero rounds: in the deterministic port numbering model, the initial knowledge of the nodes is the same. Hence, all nodes must output the same configuration, in the same way (i.e., all nodes are going to output the same label on, e.g., their first port).
Note that the white nodes do not know the port numbering that the black nodes have towards them. Hence, if white nodes output a configuration $C_W$, we can force, on a black node, an arbitrary configuration $C_B$ that uses the labels appearing in $C_W$.
This gives a simple way to detect whether a problem in the black-white formalism can be solved in $0$ rounds. 
\begin{framed}
    A problem can be solved in zero rounds if and only if there exists a white configuration $\ell_1 \s \ldots \s \ell_\Delta$ satisfying that, each sub-multiset $C$ of size $\delta$ of $\{\ell_1,\ldots,\ell_\Delta\}$ is a valid black configuration.
\end{framed}
\subsubsection{Example: Bipartite Sinkless Orientation}
Recall that BSO is the following problem:
\begin{flalign*}
& \A \s \A\B^2 &\\
\\
& \B \s \A\B^2 &
\end{flalign*}
Let us see why this problem cannot be solved in $0$ rounds.
All white nodes must output the same configuration, which is any configuration that can be picked from the condensed configuration $ \A \s \A\B^2$. Any such choice must contain the label $\A$ at least once. This means that we can force $3$ white nodes to output label $\A$ towards the same black node. This black node is going to have the configuration $\A^3$, which is not allowed by the black constraint.

\paragraph{Exercise.}
Input the problem on \href{\retor}{Round Eliminator}. First, observe that, by pressing \retorcommand{Start}, the problem becomes:
\begin{flalign*}
& \A \s \B^2 &\\
\\
& \B \s \A\B^2 &
\end{flalign*}
This is because Round Eliminator notices that some configurations are useless: e.g., any white node outputting $\A^3$ could change its output to $\A \s \B^2$ while still satisfying the black constraint. It is somehow the same as discarding non-maximal configurations when computing $\re(\Pi)$. This is detected by using the so-called \emph{diagram} of the problem. We will discuss it later.

Then, observe that Round Eliminator is not computing whether the problem is solvable in $0$ rounds. Press \retorcommand{Tools $\rightarrow$ Operations $\rightarrow$ Maximize}. Now we see that the problem is not $0$ rounds solvable. Solution \href{\retor#}{here}.


\subsubsection{Example: Bipartite Maximal Matching}
Recall that we encode BMM as follows:
\begin{flalign*}
& \M \s \O^2 &\\
& \P^3 &\\
\\
& \M \s \O\P^2 &\\
& \O^3 &
\end{flalign*}
Here we have two cases to consider, either all white nodes output $\M \s \O^2$, or they all output $\P^3$. In the former case, we can force a black node to ``receive'' two $\M$s, which is not allowed. In the latter case, we can force the configuration $\P^3$ on a black node, which is not allowed. Hence, BMM is not solvable in $0$ rounds.

\paragraph{Exercise.}
Check it on \href{\retor}{Round Eliminator}. Solution \href{\retor#}{here}.


\subsubsection{Example: Arbitrary Orientation}
Let's consider the problem of outputting an arbitrary orientation of the edges. We can encode it as follows:
\begin{flalign*}
& \I\O^3 &\\
\\
& \I \s \O &
\end{flalign*}
That is, each edge must be properly oriented (if it is labeled $\I$ by one endpoint then it must be labeled $\O$ by the other endpoint, and vice versa), but on a node everything is allowed ($3$ incoming, $3$ outgoing, $1$ incoming and $2$ outgoing, $2$ incoming and $1$ outgoing).
Note that, while this looks like a trivial problem, it is actually not: it cannot be solved in $0$ rounds by the nodes.
In fact, pick an arbitrary configuration from $\I\O^3$. It contains either an $\I$ or an $\O$, w.l.o.g. assume it is an $\I$. Then we can force an edge to get the configuration $\I^2$, which is not allowed.

However, now consider the same problem, but consider an edge-centric algorithm, i.e., consider the following problem:
\begin{flalign*}
& \I \s \O &\\
\\
& \I\O^3 &
\end{flalign*}
That is, we just swapped the node and the edge constraints.
Observe that, if each edge outputs $\I \s \O$, then all nodes are going to get a configuration from $\I\O^3$, and hence they are going to be happy no matter what they get. Hence, this problem can be solved in $0$ rounds. 

This highlights the fact that which part is active matters for $0$ round solvability.

\paragraph{Exercise.}
Check both problems on \href{\retor}{Round Eliminator}. Solutions \href{\retor#}{here} and \href{\retor#}{here}.
Try the following: start from the first problem and compute $\re(\Pi)$ by pressing \retorcommand{Speedup}. What do you notice? Up to renaming, the obtained problem is equal to the second discussed problem. See it \href{\retor#}{here}. It makes sense: we have a problem that we would like to solve in $0$ rounds using a node algorithm, but we cannot, because nodes need to agree on an edge orientation. This is clearly solvable with $1$ round of communication. However, we actually need ``half'' round: it would be sufficient if each ``edge'' were a computing entity that could send the messages ``$\I$'' and ``$\O$'' to its endpoints.

\subsection{Diagram}
We now discuss the concept of \emph{diagram} (or \emph{diagram of the problem}, or \emph{diagram w.r.t.\ the passive side of the problem}, or \emph{diagram w.r.t.\ the passive constraint}). 
The idea is the following: we want to compare labels, so we define a partial order on them. This order can be useful for many things:
\begin{itemize}
    \item The possible labels of $\re(\Pi)$ depend on the diagram of $\Pi$.
    \item When performing mergings of labels, we can often infer from the diagram whether a merging would not work well.
    \item How fast the number of labels grows when performing $\re$ steps often depends on the ``shape'' of the diagram.
    \item The discussed feature that automatically computes fixed points essentially requires us to \emph{guess} the diagram that the fixed point is going to have (and we have some good default diagrams to try).
    \item We can tell Round Eliminator to automatically perform mergings of labels as a function of how they look in the diagram. Many known lower bounds can be obtained by following this idea with a fixed rule.
\end{itemize} 

Let $C$ be a constraint. We now see how to define $D$ w.r.t.\ $C$. Unless specified differently, the diagram of a problem $\Pi$ is the diagram w.r.t.\ the passive constraint of $\Pi$.
Let $\ell$ and $\ell'$ be two labels appearing in $C$. We say that $\ell \le \ell'$ if and only if, for any configuration $\ell \s \ell_2 \ldots \ell_\Delta \in C$, there exists the configuration $\ell' \s \ell_2 \ldots \ell_\Delta \in C$. In other words, $\ell \le \ell'$ if and only if, by taking an arbitrary configuration in $C$ and replacing an arbitrary amount of $\ell$ with $\ell'$, we still obtain a valid configuration.
In order to nicely see this partial order between labels, we take the Hasse diagram of the defined partial order (where we merge labels satisfying $\ell \le \ell'$ and $\ell' \le \ell$ into a single node), that is, we proceed as follows:
\begin{itemize}
    \item First, create a directed graph $D'$ in which we put an arrow from $\ell$ to $\ell'$ if and only if $\ell \le \ell'$.
    \item Then, create $D''$ by merging strongly connected components of $D'$ into a single node.
    \item Finally, obtain $D$ from $D''$ by discarding from $D''$ the edges that can be obtained via transitivity, i.e., perform transitive reduction.
\end{itemize}
Labels that correspond to the same strongly connected component of $D'$ are called \emph{equivalent}: we could replace one with the other and vice versa, and Round Eliminator suggests to merge such labels (merging such labels does not alter the time complexity of the problem).
It is typically the case that there are no equivalent labels. In that case, each node of $D$ would correspond to a set containing a single label. Hence, by a slight abuse of notation, we will also consider the nodes of the diagram as \emph{labels} (instead of \emph{sets of labels}).

\subsubsection{Example: MIS}
Recall that we encode MIS (on $3$-regular graphs) as follows:
\begin{flalign*}
& \M^3 &\\
& \P \s \O^2 &\\
\\
& \M \s \O\P &\\
& \O \s \O &
\end{flalign*}
We can see that $\P \le \O$. In fact, the only passive configuration containing $\P$ is $\M \s \P$, and by replacing $\P$ with $\O$ we obtain $\M \s \O$, which is allowed by the passive constraint.
We can see that $\O \not\le \P$ because of the configuration $\O^2$: by replacing, e.g., one $\O$ with $\P$ we obtain $\O \s \O$ which is not allowed.
We can see that $\M \not\le \O$ and $\M \not\le \P$ because of $\M \s \P$ (in fact, $\O \s \P$ and $\P \s \P$ are not allowed).
We can see that $\O \not\le \M$ and $\P \not\le \M$ because of $\M \s \O$ and $\M \s \P$ (in fact, $\M \s \M$ is not allowed).

However, observe since $\P \le \O$, it holds that $\M \not\le \O$ implies $\M \not\le \P$ because of transitivity. Hence we could have skipped some checks.

\paragraph{Exercise.}
Input the problem on \href{\retor}{Round Eliminator}. Check the \retorcommand{Diagram} tab and see that there is the same diagram that we computed (see it \href{\retor#}{here}).

\subsubsection{Example: Arbitrary Orientation}
Consider the problem of computing an arbitrary orientation, and in particular its edge-centric version.
\begin{flalign*}
& \I \s \O &\\
\\
& \I\O^3 &
\end{flalign*}
It clearly holds that $\I \le \O$ and that $\O \le \I$. That is, $\I$ and $\O$ are equivalent. Hence, the problem has the same complexity of the following problem (obtained by merging $\O$ with $\I$):
\begin{flalign*}
& \I^2 &\\
\\
& \I^3 &
\end{flalign*}
After this merging, it is easier to see that the problem is solvable in $0$ rounds.

\paragraph{Exercise.}
Input the problem on \href{\retor}{Round Eliminator}. Observe the diagram. It is a single node containing both $\I$ and $\O$, so the labels are equivalent. Round Eliminator also gives the message ``The following labels can be merged: IO''. We can perform the suggested merging by pressing \retorcommand{Tools $\rightarrow$ Operations $\rightarrow$ Merge}.
It always makes sense to merge equivalent labels: the problem gets smaller so easier to understand, while keeping the same complexity of the original one. See the result \href{\retor#}{here}.

\subsubsection{Maximized Constraint}
In the case of MIS it was easy to see the label relation. The passive constraint is written as two \emph{condensed} configurations, $\M \s \O\P$ and $\O^2$, so it is easy to see that we can always replace $\P$ with $\O$: every time $\P$ appears in a disjunction, $\O$ also appears in the same disjunction. Please note that this is not necessarily always the case. We could have written the passive constraint of MIS as:
\begin{flalign*}
& \M\O \s \O &\\
& \P \s \M &
\end{flalign*}
Here it is slightly harder (by a simple visual inspection) to detect that $\P \le \O$. 
There is a way to write the passive constraint so that it makes it easier to compute the diagram. This version is called \emph{maximized} version of a constraint. Maximized constraints are both used in Round Elimination proofs, and by Round Eliminator to more efficiently compute the diagram.
The maximized version of a constraint is obtained by computing the active constraint of $\re(\Pi)$ and then treating each set of labels as a disjunction in a condensed configuration. For example, the active constraint of $\re(\mathrm{MIS})$ (i.e., the result of applying the universal quantifier and discarding non-maximal configurations) would be the following:
\begin{flalign*}
& \{\O\} \s \{\M,\O\} &\\
& \{\M\} \s \{\P,\O\} &
\end{flalign*}
The maximized version of the passive constraint of MIS hence is the following:
\begin{flalign*}
& \O \s \M\O &\\
& \M \s \P\O &
\end{flalign*}
This form \emph{guarantees} that $\ell \le \ell'$ if and only if each time $\ell$ appears in a disjunction, $\ell'$ also appears in the same disjunction, which is much easier to check.
Note that, once a passive constraint of a problem $\Pi$ is maximized, computing $\re(\Pi)$ becomes trivial (we essentially already have the new active constraint, and we have seen that the new passive constraint is easy to compute).

\subsubsection{Partial Diagram}
While the diagram of a problem may be hard to compute, there is an easy way to compute a good approximation of it (and in particular a subset of its relations), which often turns out to be equal to the real diagram.
Suppose we computed $\Pi' = \re(\Pi)$, and now we want to compute the diagram of $\Pi'$. Recall that each label of $\Pi'$ is a set of labels of $\Pi$. 
\begin{framed}
    If $\ell \subseteq \ell'$, then $\ell \le \ell'$.
\end{framed}
Sometimes, when writing round elimination proofs, we do not care about the exact diagram, and we only really need the relations implied by the above statement.


\chapter{Other Lower Bound Techniques}
[TODO: Marks, KMW, indistinguishability, ...]


\backmatter

\bibliographystyle{plain}
\bibliography{da}

\end{document}
